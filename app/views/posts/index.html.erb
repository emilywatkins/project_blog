<div class="container">
  <h2>view all posts here</h2>

  <% @posts.each do |post| %>
    <%= link_to post.title, post_path(post) %>
  <% end %>
  <br>
  <% if current_user.try(:is_admin?) %>
    <%= link_to "add a new post", new_post_path %>
  <% end %>
</div>
